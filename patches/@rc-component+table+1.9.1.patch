diff --git a/node_modules/@rc-component/table/es/FixedHolder/index.js b/node_modules/@rc-component/table/es/FixedHolder/index.js
index b433f0e..de314d6 100644
--- a/node_modules/@rc-component/table/es/FixedHolder/index.js
+++ b/node_modules/@rc-component/table/es/FixedHolder/index.js
@@ -129,10 +129,10 @@ const FixedHolder = /*#__PURE__*/React.forwardRef((props, ref) => {
   }, [combinationScrollBarSize, stickyOffsets, isSticky]);
   const mergedColumnWidth = useColumnWidth(colWidths, columCount);
   const isColGroupEmpty = useMemo(() => {
-    // use original ColGroup if no data or no calculated column width, otherwise use calculated column width
-    // Return original colGroup if no data, or mergedColumnWidth is empty, or all widths are falsy
+    // use original ColGroup if no calculated column width, otherwise use calculated column width
+    // Patched: removed noData check so header always uses measured colgroup with scrollbar column
     const noWidth = !mergedColumnWidth || !mergedColumnWidth.length || mergedColumnWidth.every(w => !w);
-    return noData || noWidth;
+    return noWidth;
   }, [noData, mergedColumnWidth]);
   return /*#__PURE__*/React.createElement("div", {
     style: {
diff --git a/node_modules/@rc-component/table/es/Table.js b/node_modules/@rc-component/table/es/Table.js
index 7a4cb57..13fbae1 100644
--- a/node_modules/@rc-component/table/es/Table.js
+++ b/node_modules/@rc-component/table/es/Table.js
@@ -252,7 +252,7 @@ const Table = (tableProps, ref) => {
   let scrollTableStyle;
   if (fixHeader) {
     scrollYStyle = {
-      overflowY: hasData ? 'scroll' : 'auto',
+      overflowY: 'scroll',
       maxHeight: scroll.y
     };
   }
diff --git a/node_modules/@rc-component/table/lib/FixedHolder/index.js b/node_modules/@rc-component/table/lib/FixedHolder/index.js
index 0bb5721..50e9b59 100644
--- a/node_modules/@rc-component/table/lib/FixedHolder/index.js
+++ b/node_modules/@rc-component/table/lib/FixedHolder/index.js
@@ -138,10 +138,10 @@ const FixedHolder = /*#__PURE__*/React.forwardRef((props, ref) => {
   }, [combinationScrollBarSize, stickyOffsets, isSticky]);
   const mergedColumnWidth = useColumnWidth(colWidths, columCount);
   const isColGroupEmpty = (0, _react.useMemo)(() => {
-    // use original ColGroup if no data or no calculated column width, otherwise use calculated column width
-    // Return original colGroup if no data, or mergedColumnWidth is empty, or all widths are falsy
+    // use original ColGroup if no calculated column width, otherwise use calculated column width
+    // Patched: removed noData check so header always uses measured colgroup with scrollbar column
     const noWidth = !mergedColumnWidth || !mergedColumnWidth.length || mergedColumnWidth.every(w => !w);
-    return noData || noWidth;
+    return noWidth;
   }, [noData, mergedColumnWidth]);
   return /*#__PURE__*/React.createElement("div", {
     style: {
diff --git a/node_modules/@rc-component/table/lib/Table.js b/node_modules/@rc-component/table/lib/Table.js
index 0cc139f..f02add3 100644
--- a/node_modules/@rc-component/table/lib/Table.js
+++ b/node_modules/@rc-component/table/lib/Table.js
@@ -259,7 +259,7 @@ const Table = (tableProps, ref) => {
   let scrollTableStyle;
   if (fixHeader) {
     scrollYStyle = {
-      overflowY: hasData ? 'scroll' : 'auto',
+      overflowY: 'scroll',
       maxHeight: scroll.y
     };
   }
