# 集运系统使用说明

## 1. 文档说明

本文档介绍“CMS 跨境系统”当前后台各模块的用途和基本操作，适用于平台管理员及物流商后台账号。

当前系统仅提供后台登录入口，普通会员没有独立的会员登录页面。会员资料、订单、包裹和地址等信息由具备相应权限的后台账号维护。后续如上线会员端，应另行补充会员端使用说明。

> 系统实际显示的菜单和按钮以当前账号权限为准。本文提到的功能若未显示，请联系角色管理员检查权限配置。

## 2. 账号类型与数据范围

### 2.1 平台账号

平台账号用于系统级管理。根据所授予的权限，可查看和维护平台数据、物流商、平台管理员、物流商管理员、角色权限及各业务模块。

部分业务表单会显示“物流商”字段。平台账号应根据数据归属选择对应物流商；未指定物流商的数据可能作为平台级数据使用，具体以表单要求为准。

### 2.2 物流商账号

物流商账号归属于一个物流商，原则上只能查看和操作本物流商范围内的数据。菜单、按钮及可执行操作由角色权限决定。

物流商账号可在获得授权后查看其他物流商授权的航线相关信息，但不能修改或删除不属于本物流商的数据。

### 2.3 普通会员

普通会员是后台“会员管理”中的业务对象，当前不能直接登录本系统。会员资料由后台账号维护，相关订单、包裹和地址簿记录可与会员关联。

## 3. 登录与界面

### 3.1 登录系统

1. 在浏览器打开系统地址。默认本机地址为 `http://localhost:3002`，实际部署环境请使用管理员提供的地址。
2. 输入后台账号和密码。
3. 单击“登录”。
4. 登录成功后，系统进入当前账号有权访问的第一个页面。

登录失败时，请按页面提示处理：

- 提示账号或密码错误：重新核对账号、密码和输入法状态。
- 提示请求过于频繁：等待页面提示的时间后再试。
- 提示网络错误：检查网络连接和系统服务是否正常。

系统默认在长时间无操作后自动退出，默认空闲时长为 60 分钟，具体以部署配置为准。

### 3.2 界面组成

- 左侧菜单：进入概览、包裹、订单、会员和系统管理等模块。单击左上角折叠按钮可收起或展开菜单。
- 顶部区域：显示当前模块、刷新按钮、当前角色、账号和所属平台或物流商。
- 账号菜单：单击右上角账号区域，可退出登录。
- 语言切换：在左侧菜单底部切换简体中文、繁体中文、英文或印尼文。
- 刷新按钮：重新加载当前模块数据。编辑中的未保存内容不会保留。

## 4. 通用操作

多数列表模块采用相同的操作方式：

- 综合搜索：在列表上方输入关键词后按回车或单击搜索按钮。
- 列搜索：在表头下方的输入框中按具体字段筛选。
- 日期筛选：选择起止日期，筛选指定时间范围内的数据。
- 状态筛选：选择启用、停用、未删除或已删除等状态。
- 排序：单击支持排序的表头切换升序或降序。
- 重置筛选：单击操作列表头中的刷新图标，清除当前模块的搜索和筛选条件。
- 分页：在列表底部切换页码或每页显示条数。
- 查看：单击行内查看按钮打开详情。
- 新增或入库：填写带必填标记的字段后确认提交。
- 修改：单击行内编辑按钮，修改后保存。
- 删除：单击行内删除按钮并二次确认。
- 批量删除：先勾选数据，再单击“批量删除”并确认。

删除前应确认数据不再使用。部分模块使用硬删除，删除后不能恢复；部分历史列表会保留已删除标记，但仍不应将删除当作临时隐藏功能。

## 5. 功能模块

### 5.1 概览

概览用于查看会员总数、订单总数和包裹总数，并显示 API 服务、数据库连接及应用状态。该页面主要用于快速了解系统整体情况，不提供数据编辑功能。

### 5.2 包裹管理

包裹管理用于完成包裹入库、资料维护、状态更新、查询和导出。

主要操作：

1. 单击“入库”，填写包裹单号、重量、长宽高等必填信息。
2. 根据需要填写库位号、物流商并上传包裹图片。
3. 单击“确认入库”保存包裹。
4. 使用综合搜索、列搜索、日期筛选或状态快筛查找包裹。
5. 在货物态或信息态快筛栏中可同时选择多个状态，状态下方数字为对应包裹数量。
6. 使用行内修改按钮维护包裹资料；具有状态权限的账号可调整包裹状态。
7. 勾选多条包裹后，可批量删除、批量修改物流商或批量修改货物态。
8. 单击“下载”导出数据。已勾选数据时导出所选记录；未勾选时导出当前筛选结果。

包裹单号应准确填写。批量修改物流商或货物态会同时影响所有已选记录，提交前应复核选择数量和目标值。

### 5.3 订单管理

订单管理用于查看会员订单、检索订单、更新订单状态和删除订单记录。

可通过订单编号、会员、金额、状态、创建时间等列表字段进行搜索、筛选或排序。具有状态修改权限的账号可更新订单状态；删除和批量删除仅对具备删除权限的账号显示。

### 5.4 航线运输管理

航线运输管理包含四个子页签，四个页签共用航线运输模块的查看、新增、修改和删除权限。

#### 5.4.1 提（运）单管理

用于维护提运单号、关联班（航）次、起运港、目的港、发货人、收货人、通知方、交货地点、集装箱、封条、件数、重量、体积、唛头、货物状态和备注等信息。

新增或编辑时，可选择可用班（航）次，系统会带出对应港口供选择。物流商账号可选择本物流商班次，也可选择通过航线授权获得的班次。集装箱号可从现有启用集装箱中选择，也可按业务要求录入。

#### 5.4.2 班（航）次管理

用于维护班次名称、班次号、关联航线、预计及实际启航/到港时间、截单时间、交货截止时间、截 VGM 时间、港口和启用状态。

选择关联航线后，系统会默认带出该航线的起运港和目的港。物流商账号只能修改或删除本物流商拥有的班次；被授权航线下的其他物流商班次仅供查看和关联使用。

#### 5.4.3 航线管理

用于维护航线名称、代码、承运类型、承运工具、承运人、起运港、目的港、启用状态和备注。多个港口可使用 `/` 分隔。

航线所有者可通过“授权”操作将航线授权给其他物流商。授权后，对方可只读查看该航线下班次，并可在提（运）单中关联相应班次。修改授权名单后单击“保存授权”。

#### 5.4.4 集装箱管理

用于维护箱号、箱型、启用状态和备注。常见箱型可按实际业务填写，例如 `20GP`、`40HQ` 或 `45HQ`。新增时应确保箱号准确且不重复。

### 5.5 简讯管理

简讯管理用于查看短信验证码记录，包括手机号、验证码、验证状态、有效期和创建时间等信息。该模块支持搜索、日期筛选、单条删除和批量删除，不提供新增或修改操作。

验证码属于敏感信息，仅应向确有业务需要的角色授予查看权限。

### 5.6 物流商管理

物流商管理用于维护物流商名称、代号、联系人、电话、电子邮箱、网站、状态等资料。

可新增、查看、修改、删除或批量删除物流商。物流商代号会用于生成物流商管理员账号后缀，因此创建后应谨慎修改。删除物流商前，应先确认其管理员、会员、包裹、库位和运输数据的处理方式。

### 5.7 库位管理

库位管理用于维护仓库中的库位及其尺寸和容量信息。

新增库位时，按表单填写区域、排、架、层、位，系统会组合并预览库位号；同时可填写长、宽、高、容量、仓库、说明和启用状态。平台账号可根据表单选择所属物流商，物流商账号的数据自动限制在本物流商范围内。

库位支持查看、修改、删除、批量删除、搜索、筛选和排序。库位编号应与现场标识保持一致。

### 5.8 会员管理

会员管理用于查看和维护普通会员资料，包括用户名、手机号、电子邮箱、OCR 识别码、真实姓名、地址及创建时间等。同一物流商下，用户名、手机号、电子邮箱和识别码均不能重复。

当前模块支持新增、查看、修改、删除和批量删除，不提供会员自行登录或自行修改资料的入口。新增会员保存时，系统必须自动生成识别码，操作人员无需且不能手动指定。修改会员关键信息前，应确认不会影响其订单、包裹和地址簿关联。

识别码为固定 6 位，由字符集 `37AEFHJKMPRTWXY` 中的 5 位主体码和 1 位校验位组成。系统按《OCR识别码使用说明》的加权模 15 规则生成；识别码与会员终身绑定，创建后只读，不允许清空或重新生成。

### 5.9 地址簿

地址簿用于维护收发件联系人地址，可关联会员。

新增地址时：

1. 选择所属物流商；物流商账号自动使用本物流商。
2. 按需搜索并选择关联会员。
3. 填写姓名和电话号码。
4. 依次选择省、市、区县及街道或乡镇；港澳台地区按页面提供的层级选择。
5. 填写详细地址后保存。

系统会根据省级区域识别中国大陆、台湾、香港或澳门，并显示相应电话区号。地址簿支持查看、修改、删除、批量删除和多字段检索。

### 5.10 单号库

单号库用于管理号段及号段内的具体单号。

号段列表显示号段名称、库存数量、预计用尽天数、启用状态和归属物流商。主要操作如下：

1. 新增号段并填写名称、说明和启用状态。
2. 进入号段的单号管理窗口。
3. 单击“导入单号”，使用换行或逗号分隔多个单号后提交；单次最多导入 10,000 条。
4. 按未使用或已使用状态筛选单号，也可搜索具体单号。
5. 删除单条单号，或勾选后批量删除。

库存数量为未使用单号数。预计用尽天数根据近期使用速度计算；近期没有消耗时可能不显示预计天数。当前系统主要提供号段、单号的维护能力，单号自动标记为已使用取决于后续业务流程是否已接入。

### 5.11 系统设置

系统设置根据权限显示“包裹状态字典”和“标签管理”页签。

#### 5.11.1 包裹状态字典

用于维护包裹的货物态和信息态，包括状态编码、状态名称、类型、排序和启用状态。新增或修改状态后，包裹管理等使用状态字典的页面会采用新配置。

状态编码可能被现有包裹记录引用。删除状态前，应先确认不会造成历史数据无法正确显示。

#### 5.11.2 标签管理

用于维护标签名称、HTML 模板、说明、启用状态和归属物流商。

可手动编辑模板内容，也可上传 `.html` 文件导入模板。保存前可预览当前模板；列表中可直接预览或打印。打印时浏览器可能拦截新窗口，请允许系统地址打开弹出窗口后重试。

标签 HTML 应仅由可信管理员维护，避免导入来源不明的脚本或外部资源。

### 5.12 系统管理员

系统管理员根据权限显示以下页签：

- 平台管理员：管理平台范围的后台账号，仅平台账号可见。
- 物流商管理员：管理归属于物流商的后台账号。
- 平台权限：管理平台角色及其权限。
- 物流商权限：管理物流商角色及其权限。

#### 5.12.1 管理员账号

新增管理员时，选择角色、归属范围、所属物流商并设置账号资料。新增物流商管理员时，账号基础名称仅使用字母和数字，系统会自动拼接 `@物流商代号`，例如 `operator@abc`。

可根据权限修改管理员资料、启停账号或删除账号。物流商初始管理员等系统账号可能不允许删除。物流商账号只能管理本物流商范围内的管理员，不能创建平台管理员或跨物流商操作。

#### 5.12.2 角色权限

角色用于集中分配菜单和操作权限。新增或编辑角色时：

1. 选择平台角色或物流商角色。
2. 填写角色名称及相关资料。
3. 按业务职责勾选查看、新增、修改、状态修改、删除、导出等权限。
4. 保存后，将角色分配给相应管理员。

应遵循最小权限原则，只授予完成岗位工作所需的权限。系统内置角色可能不允许删除。调整角色权限会影响使用该角色的所有管理员，应在保存前复核。

## 6. 权限与数据安全注意事项

- 看不到菜单：当前角色没有该模块的查看权限。
- 看得到列表但没有新增、修改或删除按钮：当前角色只有查看权限，或缺少对应操作权限。
- 物流商账号看不到其他物流商数据：这是正常的数据隔离行为。
- 授权航线只赋予规定范围内的查看和关联能力，不转移数据所有权。
- 删除、批量修改、批量删除和角色权限调整均属于高风险操作，执行前应复核对象和数量。
- 短信验证码、会员联系方式、收发件地址等属于敏感信息，不应通过无关渠道传播。
- 使用完毕后应从右上角账号菜单退出，尤其是在共用电脑上操作时。

## 7. 常见问题

### 7.1 登录后没有进入概览页

系统会自动进入当前账号有权访问的第一个页面。没有概览权限时不会显示概览菜单，这是正常现象。

### 7.2 页面没有某个按钮

按钮由独立权限控制。例如能查看包裹不代表可以入库、修改、删除或导出。请联系角色管理员核对对应操作权限。

### 7.3 搜索后没有数据

先单击操作列表头中的重置图标清除列筛选，再清空综合搜索和日期条件。如果仍无数据，请确认账号所属物流商及数据归属是否正确。

### 7.4 列表没有显示刚保存的数据

单击顶部刷新按钮重新加载当前模块。仍未显示时，请检查是否启用了筛选条件，或数据是否被保存到其他物流商范围。

### 7.5 标签无法打开打印窗口

浏览器可能拦截了弹出窗口。请允许当前系统地址打开弹出窗口，然后重新单击打印。

### 7.6 普通会员如何登录

当前版本没有普通会员登录端。普通会员资料由后台账号在会员管理、地址簿、订单和包裹等模块中维护。

---

文档依据当前系统界面编写。系统功能、字段或权限发生调整后，应同步更新本文档。